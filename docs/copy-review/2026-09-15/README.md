# Copy Review Export — 2026-09-15

本目录是谢邀喵当前固定页面文案的润色交接包。

- `fixed-copy-catalog.md`：分页面总表。
- `fixed-copy-catalog.csv`：建议新增 `suggested_text` / `reason` 两列进行润色。
- `fixed-copy-catalog.json`：程序化处理。
- `page-context.md`：产品语义、事实边界、各页面职责。

当前共导出 **690** 条固定可见文案/可见兜底文案。

润色时不要覆盖 `original_text`。
